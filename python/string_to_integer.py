# Challenge: String to Integer Conversion

# Write a function `string_to_integer(s: str) -> int` that takes a string representing an integer (e.g., `"123"`, `"-456"`) and returns the corresponding integer.
# You must implement this conversion **without using the `int()` function** or any other built-in function that directly performs this conversion.

# **Assumptions:**

# 1. The input string `s` will always be positive
# 2. The input string, when converted, will be a valid integer
# 3. You may assume that the input will always fit within a standard 32-bit integer range.

# **Examples:**
# `string_to_integer("123")` returns `123`

def string_to_integer(s: str) -> int:
    return 0